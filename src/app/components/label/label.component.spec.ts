import { LabelComponent } from './label.component';

describe('LabelComponent', () => {
  it('should create an instance', () => {
    const directive = new LabelComponent();
    expect(directive).toBeTruthy();
  });
});
